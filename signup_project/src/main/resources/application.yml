spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/signup?characterEncoding=UTF-8&serverTimezone=Asia/Seoul&useUnicode=true
    username: root
    password: Money12030
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MariaDBDialect
